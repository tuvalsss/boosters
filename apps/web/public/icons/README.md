# PWA icons

- `icon-192.png` — 192×192
- `icon-512.png` — 512×512 (used for maskable + splash)

The checked-in icons are generated brand marks that satisfy the manifest. To
regenerate them after a brand refresh, run:

```bash
pnpm assets:icons
```
