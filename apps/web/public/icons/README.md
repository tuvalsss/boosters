# PWA icons

Replace these placeholders with real branded icons before any production build:

- `icon-192.png` — 192×192
- `icon-512.png` — 512×512 (used for maskable + splash)

Phase 1 ships without binary assets to keep the repo lean; the manifest
references these paths so installability works once the icons are added.
